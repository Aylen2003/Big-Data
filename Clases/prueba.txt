data_grouped = data.groupby('class')

data_grouped.get_group('First')

for key, group in data_grouped:
    print(key)
    display(group.head(3))

Hacer una tabla pivot que la fracción de sobrevivientes y valor medio de la tarifa segun la clase y el sexo
